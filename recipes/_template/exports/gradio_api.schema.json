{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-oss-harness/recipes/{RECIPE_NAME}/gradio_api.schema",
  "title": "{RECIPE_NAME} Gradio endpoint contract",
  "description": "Auto-generated by tools/generate_export.py. Spring backend treats this as the source of truth. Colab ships Gradio 5.x — the wire protocol is a two-step queued call (POST /call/<fn> -> event_id, then GET /call/<fn>/<event_id> via SSE), NOT the legacy /run/predict one-shot.",
  "type": "object",
  "properties": {
    "endpoint": {
      "type": "object",
      "properties": {
        "submit": {
          "type": "object",
          "properties": {
            "method": { "const": "POST" },
            "path": {
              "type": "string",
              "const": "/call/predict",
              "description": "Gradio 4+/5.x queued-call submit endpoint. Returns {event_id}."
            }
          }
        },
        "stream": {
          "type": "object",
          "properties": {
            "method": { "const": "GET" },
            "path": {
              "type": "string",
              "const": "/call/predict/{event_id}",
              "description": "SSE stream. Consume events until msg=process_completed; output.data[...] is the final result."
            }
          }
        },
        "url_env_var": {
          "type": "string",
          "const": "{BACKEND_ENV_VAR}",
          "description": "Backend reads the actual Gradio share URL host from this env var."
        }
      }
    },
    "request": {
      "type": "object",
      "description": "Gradio wraps positional inputs as {\"data\": [arg1, arg2, ...]}.",
      "properties": {
        "data": {
          "type": "array",
          "description": "Position-based input list — matches the parameter order of inference_handler.infer().",
          "items": {
            "oneOf": [
              { "type": "string", "description": "text input" },
              { "type": "string", "format": "data-url", "description": "base64 data URL for image/audio/video" },
              { "type": "number" },
              { "type": "boolean" },
              { "type": "object", "description": "structured input (Gradio JSON component)" }
            ]
          }
        }
      },
      "required": ["data"]
    },
    "response": {
      "type": "object",
      "description": "Final SSE event payload when msg=process_completed.",
      "properties": {
        "msg": { "const": "process_completed" },
        "output": {
          "type": "object",
          "properties": {
            "data": {
              "type": "array",
              "description": "Position-based output list — matches return values of inference_handler.infer().",
              "items": { "$comment": "TODO recipe author: replace with actual output shape" }
            },
            "duration": { "type": "number" }
          }
        }
      }
    },
    "errors": {
      "type": "object",
      "description": "Failure modes the backend must distinguish.",
      "properties": {
        "gradio_url_unset": { "description": "{BACKEND_ENV_VAR} env not set — backend should return 503 to frontend." },
        "gradio_url_expired": { "description": "Colab session ended — share URL invalid. Backend retries against newest URL from {TEAM_CHANNEL}." },
        "vram_oom": { "description": "Model returned OOM — backend should return 413." },
        "model_cold_start": { "description": "First request after Colab restart can take 30-60s. Backend timeout should be >= 90s." },
        "sse_incomplete": { "description": "SSE stream closed without emitting msg=process_completed — upstream crashed mid-inference; treat as 502." }
      }
    },
    "_template_marker": {
      "description": "Recipe authors: edit the `request.data.items` and `response.output.data.items` shapes to match your actual inference_handler.infer() signature. The generator only fills RECIPE_NAME and structural defaults; semantics are yours.",
      "type": "string"
    }
  },
  "required": ["endpoint", "request", "response"]
}
