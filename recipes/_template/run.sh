#!/usr/bin/env bash
# Run the recipe
set -euo pipefail

RECIPE_DIR="$(cd "$(dirname "$0")" && pwd)"

echo "[run] Recipe: _template"
echo "[run] This is a skeleton — replace this with your actual run command."
echo "[run] Example: python \$RECIPE_DIR/main.py"
