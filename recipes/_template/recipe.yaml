name: _template
version: "0.1.0"
description: >
  Skeleton recipe — copy this folder to start a new OSS Colab test.

# Which requirements file to use (opt1 = minimal, opt2 = full)
requirements: requirements_opt1.txt

# Entry point
main: run.sh

# Colab notebook generation
notebook_manifest: notebook_manifest.yaml

# Docs triad (SSOT)
docs:
  plan: docs/plan.md
  context: docs/context.md
  tasks: docs/tasks.md
