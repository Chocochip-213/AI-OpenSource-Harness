# Model Card — {RECIPE_NAME}

> Auto-generated by `tools/generate_export.py` from `recipe.yaml` +
> `notebook_manifest.yaml`. Edit by hand only fields that the generator
> didn't fill (marked `<!-- FILL -->`).

## Identity
- **Recipe**: `{RECIPE_NAME}`
- **Version**: `{RECIPE_VERSION}`
- **Upstream**: `{UPSTREAM_REPO}` @ `{UPSTREAM_REF}`
- **License**: <!-- FILL — upstream model license, e.g. Apache-2.0, MIT, CC-BY-NC -->

## What it does
<!-- FILL — one paragraph: input → output, in plain language.
e.g. "Single photo of a person → rigged GLB avatar (gltf 2.0)." -->

## I/O contract
- **Input**: <!-- FILL — type + shape + range, e.g. "RGB image, 512×512, uint8" -->
- **Output**: <!-- FILL — same format as `integration.asset_mime` in recipe.yaml -->
- **JSON schema**: see `gradio_api.schema.json`

## Runtime requirements
- **GPU**: `{PREFERRED_GPU}` recommended (any Colab GPU with VRAM ≥ `{VRAM_MIN_GB}` GB works)
- **VRAM**: `{VRAM_MIN_GB}` GB minimum (hard check)
- **Python**: `{PYTHON_VERSION}`
- **Colab runtime**: `{COLAB_VERSION}`

## Performance (measured)
<!-- FILL after first successful run — record metrics so backend can plan timeouts -->
- Cold-start time (model load): <!-- e.g. 45 sec on A100 -->
- Inference latency (single request): <!-- e.g. 2.3 sec -->
- VRAM peak: <!-- e.g. 18 GB / 40 GB total -->
- Throughput (batch=1, sustained): <!-- e.g. 0.4 req/sec -->

## Known limitations
<!-- FILL — anything that callers MUST know to avoid surprises:
e.g. "Faces with sunglasses cause artifacts on the eye region."
e.g. "Input shorter than 3 seconds returns empty audio." -->

## Failure modes the backend must handle
- **GPU not allocated** (Colab cold-start) → 503-equivalent in Gradio response
- **Gradio share URL expired** (Colab session ended) → reconnect SOP in `INTEGRATION_BACKEND.md`
- **Out of VRAM** (input too large) → 413-equivalent
