name: _template
version: "0.1.0"
description: >
  Template recipe. Copy this to start a new recipe.

upstream:
  repo: https://github.com/org/repo.git
  ref: main  # Pin to specific commit SHA for reproducibility
  branch: main

requirements: requirements_opt1.txt

main: run.sh

notebook_manifest: notebook_manifest.yaml

docs:
  plan: docs/plan.md
  context: docs/context.md
  tasks: docs/tasks.md

runtime:
  gpu: A100
  python: "3.11"
  disk_gb: 20
  vram_min_gb: 24
